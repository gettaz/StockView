export const environment = {
    production: true,
    firebaseConfig: {
      // Your Firebase configuration for the production environment
      // This includes apiKey, authDomain, projectId, etc.
    }
  };
  